import React from 'react';

import { Typography } from '@material-ui/core';

const MapView = () => (
  <Typography variant="display1" className="dashboardComponent">
    MapView
  </Typography>
);

export default MapView;
